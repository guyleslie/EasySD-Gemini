# Build artifacts
IRQHack64/build/
Arduino/IRQHack64/build/
EasySD/build/
Arduino/EasySD/build/
build/

# Generated Arduino headers (copied from build/artifacts/)
Arduino/IRQHack64/FlashLib.h
Arduino/IRQHack64/BuildConfig.h
Arduino/EasySD/FlashLib.h
Arduino/EasySD/BuildConfig.h


# Visual Studio user files
*.csproj.user
*.suo
*.user
obj/
bin/

# IDE
.vscode/

# OS files
Thumbs.db
.DS_Store
nul

# SD card media files (large binaries, generated locally)
sdcard/*.CVD
sdcard/*.cvd

# Multiload game images (commercial ROMs — kept locally only).
# Legacy local-only folder; the MultiLoad feature has been removed but the
# folder name is preserved in .gitignore so any leftover binaries on the
# user's working copy stay out of commits.
/multiload/

# Charset preview working files (commercial font extracts)
tmp_charset_preview/

# Backup archives
*.zip

# Local-only archived material
[Aa]rchive/
[Aa]rchived/
_archive/

# Local-only workspace files
_ul

# Old backup files
*.old
.claude/
# Python cache
Tools/__pycache__/
